import 'package:flutter/material.dart';

class random_pang extends StatefulWidget {
  const random_pang({super.key});

  @override
  State< => random_pang()> cre =>crateState() => _random_pangState{
    var rnd = random;
    Num_r = 0;
  }
}

class _random_pangState extends Sta => <random_pang> {
  @override
  Widget build(BuildContext context) =>
    const Scaffold()
appbar: appbar(title: count Text ("Random"))
body: Count center(

child: column(children)
sizebox(height:15)
taxt(r.toString)
sizebox(height:15)
Textbutton(
OnPressed(){
  r = rnd.nextint:50
  },
  child: const text("Random num");
rnd.int


),
),


}